"""Billing domain — service, webhook handler, operations, repository, and protocols.

Use Inject(BillingServiceProtocol) or Inject(BillingWebhookProtocol) in
FastAPI endpoints to get the container-managed instances.
"""
